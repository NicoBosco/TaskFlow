# Configuración de Entorno de Pruebas (Test Environment)
# Para ejecutar npm test, copia este archivo a .env.test y ajusta los valores.

NODE_ENV=test
PORT=3001
# IMPORTANTE: Usa una DB dedicada para tests, ya que se vacía en cada ejecución.
DATABASE_URL=postgresql://postgres:Castleofglash001%3A.@localhost:5432/taskflow_test
JWT_SECRET=super_secret_test_key_minimum_length_10
FRONTEND_URL=http://localhost:3000
